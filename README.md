# DAV-PRACS


GENERAL INSTRUCTIONS:
1) Files with name as DAV_exptname can be directly opened in google colab, eg. DAV_Linear
2) Files with name as just exptname contains only the code which is seperated by leaving lines in between indicating different cells which can be run on google colab, eg. LINEAR


FOR EXPT 1: 
There are two files sperating R and Python Libraries (named LIBRARIES PYTHON and LIBRARIES R)
But there are various libraries in which problem occurs when they are run in google colab so I suggest if you got expt 1 then do not prefer my files
The libraries which are not covered in my files are:

i) PyCaret (Python Library) -> This need older version on python which is not there in my device so it is not included.
ii) Shiny, DT, knitr, R Markdown, Leaflet, Caret (R Libraries) -> These need package installations and other problems were also occuring so these are not performed.

***To run R Libraries, perform the below steps in google colab:
Runtime -> Change runtime type -> R
